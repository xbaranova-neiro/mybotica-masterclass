# MyBotica master-class

Static landing page for the MyBotica master-class on 24 September 2026 at 19:00.

The registration widget is intentionally not included yet. Its container is `#registration-widget` in `index.html`. The guide cover is a visual preview only; the PDF gift is delivered in the channel after registration and is not stored in this public repository.

Open `index.html` locally or serve this directory with any static web server.
