# MyBotica master-class

Static landing page for the MyBotica master-class on 24 September 2026 at 19:00.

The GetCourse registration widget is embedded in an accessible modal and opens from every registration button without leaving the page. The guide cover is a visual preview only; the PDF gift is delivered in the channel after registration and is not stored in this public repository.

Open `index.html` locally or serve this directory with any static web server.
